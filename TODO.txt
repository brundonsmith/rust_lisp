- Add RefCell inside ConsCells so that they can be mutated (and then implement 
  FromIterator and possibly push() method)
- Macros
- Backtick (with escapes)
- Rest arguments
- Optional arguments
- Some kind of key/value structure
- Ensure reference cycles are handled
- Rust macro for interop
- Make strings immutable?