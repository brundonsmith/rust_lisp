MVP:
- Continue adding basic functions/forms:
  - set
  - macros
  - timestamp
- backtick shortcut
- some kind of key/value structure
- More closely adhere to either scheme or common-lisp syntax

Other:
- Handle reference cycles
- Tail recursion
- Better parsing strategy
- Find more concise/efficient way to represent native functions/polymorphism
- Rust macro for FFI
- Macros
- Optional arguments
- Rest arguments
- Use Nil instead of Option?
- Just use cons cells for native function arguments?
- Instead of vec_to_cons, make it possible to collect an iterator into a cons
- Allow single-quotes? (backticks?)